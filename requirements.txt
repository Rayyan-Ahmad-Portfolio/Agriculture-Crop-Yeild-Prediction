# AgriYield AI — Python dependencies
# Install: pip install -r requirements.txt

# ML inference API (api.py)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pandas>=2.0.0
pydantic>=2.0.0

# Harvest console UI (app.py)
streamlit>=1.28.0
requests>=2.31.0

# scikit-learn: model pickles were trained with 1.6.1.
# On Python 3.11–3.12 use: pip install scikit-learn==1.6.1
# On Python 3.14+, 1.6.1 may not install — use latest sklearn and re-export pickles, or use Python 3.12.
scikit-learn>=1.6.1
